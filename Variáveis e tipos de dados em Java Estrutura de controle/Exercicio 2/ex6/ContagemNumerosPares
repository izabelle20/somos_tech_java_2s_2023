/*Exercício 6: Contagem de Números Pares com for e while
Peça ao usuário para inserir um número inteiro positivo. Em seguida, crie dois programas: um usando um loop for e outro usando um loop while. Ambos devem contar e exibir todos os números pares de 1 até o número inserido pelo usuário. */
import java.util.Scanner;

public class ContagemNumerosPares {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.print("Digite um número inteiro positivo: ");
        int numero = scanner.nextInt();
        
        System.out.println("Usando for:");
        for (int i = 2; i <= numero; i += 2) {
            System.out.println(i);
        }
        
        System.out.println("Usando while:");
        int i = 2;
        while (i <= numero) {
            System.out.println(i);
            i += 2;
        }
        
        scanner.close();
    }
}
